import SwiftUI

extension Color {
    // Primary accent — Apple Music pink/red
    static let tuneBoxAccent = Color(red: 0.99, green: 0.22, blue: 0.37)

    // Backgrounds
    static let tuneBoxBackground = Color(.systemBackground)
    static let tuneBoxSecondaryBackground = Color(.secondarySystemBackground)
    static let tuneBoxGroupedBackground = Color(.systemGroupedBackground)

    // Labels
    static let tuneBoxPrimary = Color(.label)
    static let tuneBoxSecondary = Color(.secondaryLabel)
    static let tuneBoxTertiary = Color(.tertiaryLabel)
}
