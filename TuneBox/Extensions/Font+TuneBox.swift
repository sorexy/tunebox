import SwiftUI

extension Font {
    // Library list
    static let songTitle = Font.system(size: 17, weight: .regular)
    static let songSubtitle = Font.system(size: 15, weight: .regular)
    static let songDuration = Font.system(size: 14, weight: .regular)

    // Now Playing
    static let nowPlayingTitle = Font.system(size: 22, weight: .bold)
    static let nowPlayingArtist = Font.system(size: 20, weight: .regular)

    // Album grid
    static let albumTitle = Font.system(size: 14, weight: .semibold)
    static let albumArtist = Font.system(size: 12, weight: .regular)

    // Mini player
    static let miniPlayerTitle = Font.system(size: 15, weight: .semibold)
    static let miniPlayerArtist = Font.system(size: 13, weight: .regular)

    // Progress time labels
    static let timeLabel = Font.system(size: 13, weight: .regular)

    // Section headers
    static let sectionHeader = Font.system(size: 13, weight: .semibold)

    // Now Playing header label
    static let nowPlayingHeader = Font.system(size: 13, weight: .semibold)
}
